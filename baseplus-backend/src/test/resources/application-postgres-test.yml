spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

jwt:
  secret: baseplus-postgresql-compatibility-test-secret
